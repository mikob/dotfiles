# Miko's custom aliases
alias rm="trash-rm"
alias wf="autossh mikob@mikob.webfactional.com"
alias opn="nemo '$pwd'"
alias fnd="find ./ -iname $@"
alias ack="ack-grep"
alias ls="ls --block-size=M --color=auto"
alias info="info --vi-keys"
alias dkrm="docker ps -aq | xargs -L 1 docker stop; docker ps -aq | xargs -L 1 docker rm"

# Miko's custom shell function
function cds() { cd "$@" && ls; }

# Miko's custom open ack results in vim
function ackvim() { 
    ack "$@" && read;
    vim "-c bufdo /$@" $(ack -l $@);
}
