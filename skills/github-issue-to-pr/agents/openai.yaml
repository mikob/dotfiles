interface:
  display_name: "GitHub Issue to PR"
  short_description: "Fix oldest issues in a loop using fresh agent tasks"
  default_prompt: "Use $github-issue-to-pr to fix the current repository's oldest open issues continuously, checking every minute between jobs and starting each issue in a fresh agent task."
