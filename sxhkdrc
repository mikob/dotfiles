super + space
    pgrep rofi > /dev/null && pkill rofi || rofi -normal-window -show combi

super + j
    xdotool windowactivate $(xdotool search --desktop $(xdotool get_desktop) --class "chrome" | tail -1) && sleep 0.05 && xdotool key --clearmodifiers ctrl+shift+a
